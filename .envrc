export DATASET_PATH=""

source_env_if_exists .envrc.local

# Install kernel with project-based name
KERNEL_NAME="$(basename $(pwd))-poetry"
poetry run python -m ipykernel install --user --name="$KERNEL_NAME" --display-name="[Poetry] $(basename $(pwd))"